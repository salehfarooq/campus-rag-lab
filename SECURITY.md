# Security

Do not commit API keys, `.env` files, generated vector stores, or benchmark artifacts.

If a key was ever committed or pasted into a notebook, rotate it before publishing the repository. This project reads secrets from `.env` and includes `.env.example` only as documentation.

