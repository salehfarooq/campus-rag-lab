# Publishing

Recommended GitHub repository settings:

- Visibility: public
- Default branch: `main`
- Description: `RAG observability lab for grounded university policy QA`
- Topics: `rag`, `gemini`, `retrieval`, `streamlit`, `python`, `llm-evaluation`

Before pushing, verify:

```bash
git status --short
make test
make lint
```

